<h1>BABY SLEEP APP</h1>

<ul>
  <% @naps.each do |nap| %>
    <li>
      <%= link_to nap.title, nap%>, <%= nap.date %>
    </li>
  <% end %>
</ul>

  <div>
  <%= link_to "New Nap", new_nap_path %>
  </div>